import React from 'react'
import { HashRouter as Router } from 'react-router-dom'

import Layout from 'Layout'

const App = () => {
    return (
        <Router>
            <Layout />
        </Router>
    )
}

export default App
